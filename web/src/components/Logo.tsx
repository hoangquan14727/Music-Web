// Cloud-with-headphones mascot logo (AI-generated, see assets-src/image-prompts.mjs).
export default function Logo({ className = "" }: { className?: string }) {
  // eslint-disable-next-line @next/next/no-img-element
  return <img src="/images/logo.webp" alt="" width={256} height={256} className={className} />;
}
